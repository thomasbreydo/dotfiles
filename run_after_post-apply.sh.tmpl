#!/usr/bin/env zsh

set -ueo pipefail

{{ if .has_root }}
export HASROOT=0
{{ else }}
export HASROOT=1
{{ end }}

chezmoi cd

echo Installing homebrew
scripts/install-homebrew.sh
brew bundle

echo Installing fish
scripts/install-fish.sh
